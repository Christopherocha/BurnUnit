#! /usr/bin/env node
require('../lib').EnvCmd(process.argv.slice(2))
 `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../env-cmd/bin/env-cmd.js" "$@"
  ret=$?
else 
  node  "$basedir/../env-cmd/bin/env-cmd.js" "$@"
  ret=$?
fi
exit $ret
